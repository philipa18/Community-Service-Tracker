<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.communityservicetracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CommunityServiceTracker">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Mention activities below -->
        <!-- Could add labels for each activity but not necessary -->
        <activity
            android:name="SignInAccCreationActivity">
        </activity>
        <activity
            android:name="StudentActivity">
        </activity>
        <activity
            android:name="OrgActivity">
        </activity>

    </application>

</manifest>